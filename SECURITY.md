# セキュリティとデータ取扱い

ManualBuilderは部内利用ですが、スクリーンショットには機密情報や個人情報が入りやすいため、ソースコードと業務データを分離します。

## GitHubへ登録しないもの

- 実際の業務画面、顧客情報、個人情報を含む画像
- ManualBuilderで作成した実案件のプロジェクトデータ
- 生成されたExcel、Word、PDF
- アクセストークン、パスワード、秘密鍵、接続情報
- 実行ログや一時ファイルに含まれるローカルパス・利用者情報

サンプルが必要な場合は、架空のシステム名・氏名・金額だけで作成し、元データからの単純な黒塗りではなく内容そのものを架空データへ置き換えます。

## Office COMの安全原則

- ユーザーが開いているExcel・Wordへ接続しない
- 自分が起動したOfficeプロセスの所有PIDを確認してから設定を変える
- 所有権を証明できないOfficeプロセスへ `Quit()` や `Stop-Process` を実行しない
- 一時ファイルへ保存し、自己検査成功後だけ完成ファイル名へ移動する
- タイムアウト時に既存Officeプロセスを一括終了しない

Phase 1でOffice出力を変更する場合は、`tests/phase0` の安全条件を回帰試験として維持してください。

## 問題を見つけた場合

公開Issueへ機密情報を貼らず、部内の管理者へ非公開の経路で連絡してください。スクリーンショットやログを共有する場合も、機密情報と個人情報を除いてください。
