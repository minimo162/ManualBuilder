# ManualBuilder Phase 1 v0.21.8 再確認

## 自動テスト

```powershell
tests\phase1\run-tests.cmd
```

Excelをすべて閉じてから、次も実行します。

```powershell
tests\phase1\run-excel-test.cmd
```

## 目視確認

Excel出力を開始し、進捗画面で次を確認します。

- 準備中・作成中・安全終了中は、斜め矢印ではなく青い処理中リングが回る
- 完了時は緑のチェックになる
- 失敗時は赤い感嘆符になる
- 中止時は灰色の×になる
- 状態メッセージ、手順数、進捗率、進捗バーは従来どおり表示される
- 一覧または編集画面の左下／右下に `v0.21.8` と表示される

Word出力でも同じ状態表示になることを確認します。
