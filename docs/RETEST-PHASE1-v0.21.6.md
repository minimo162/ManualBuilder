# ManualBuilder Phase 1 v0.21.6 再確認

## 修正内容

- Excelの長文説明を、1表示行16pt＋上下余裕4ptとしてカード行へ換算
- 補足は1表示行15pt＋上下余裕4ptとして換算
- Excelの赤枠・赤矢印・番号を、アプリと同じ「画像短辺 ÷ 650、下限0.55」の表示単位へ統一
- 横長の小画像では番号直径を約26pxまで縮め、注釈だけが大きく見える問題を修正
- 通常画面では画像サイズに応じた従来の読みやすい注釈階層を維持

## 自動確認

```powershell
tests\phase1\run-tests.cmd
```

Excelをすべて閉じてから、次も実行します。

```powershell
tests\phase1\run-excel-test.cmd
```

両方とも最後に `passed` と表示されることを確認してください。

## 目視確認

1. 添付と同程度の長文をExcelへ出力し、文章の下に残る空白がv0.21.5より1〜2行程度減っていること
2. 横長の小画像に赤枠と番号を付け、アプリ画面とExcelで線幅・番号径が近いこと
3. 通常の16:9画像の注釈が過度に小さくなっていないこと
4. 補足付き手順で説明・補足の文字が欠けていないこと
5. 一覧または編集画面の左下／右下に `v0.21.6` と表示されること
